{
  "name": "tt-offer-selector",
  "version": "1.0.0",
  "description": "Cash register Offer Selector",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "lint": "polymer lint --input tt-offer-selector.html",
    "dev": "polymer serve & npm run watch",
    "test": "polymer test",
    "watch": "browser-sync start --proxy localhost:8080 --startPath '/components/tt-offer-selector/demo/' --files '*.html, *.js, demo/*' "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.16.0"
  }
}
